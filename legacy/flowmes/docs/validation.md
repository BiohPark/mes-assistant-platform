# 검증 기록

검증일: 2026-09-16. Windows / Node.js 22.22.3 / React / TypeScript / Vite.

## 자동 검증

- `npm test`: 24개 테스트 통과 (기존 도메인 14 + 모듈/마이그레이션 4 + API 5 + 요청 초기화 1).
- `npm run build`: TypeScript 검사와 프로덕션 정적 빌드 통과.
- Vitest 5.0.1 설치 완료 시 npm 진단 결과: 알려진 취약점 0개.
- Prettier로 소스와 문서 포맷 정리.

테스트 범위: 체크리스트 미완료 차단, 선택한 산출물 참조만 인계, 원본·대화 보존, 잘못된 파일 참조 거부, 건너뛰기 사유, 되돌리기 후 재검토, 잘못된 전환 방향 차단, 대기 단계 되돌리기 거부, 후속 미완료가 있는 최종 완료 차단, 구조 편집 중 추가된 대화·파일 보존, 기록이 생긴 단계 삭제 차단.

## 브라우저에서 확인한 경로

- 목록 검색: `CIP` 검색 시 해당 업무 1개 표시.
- 업무 생성 → 7번째 단계 추가 → 수동 단계로 전환 → 저장.
- FDS 대화 입력 → 샘플 응답 → 산출물 저장.
- 미완료 체크리스트가 있는 상태에서 전환 시 안내와 함께 이동 차단.
- 체크 완료 후 산출물 두 개 중 한 개만 선택하여 개발 단계의 입력으로 인계.
- 수동 개발 메모 저장 → 새로고침 후 내용 복원.
- `docs/demo-evidence.txt` 업로드 → IndexedDB 저장 → 원문 미리보기 확인.
- 건너뛰기 시 빈 사유 차단 → 사유 입력 후 전환 → 사유를 남기고 URS로 복귀.
- 완료 단계의 체크박스 비활성화 및 대기 단계의 되돌리기 비활성화.
- 주별 → 일별, 담당자 김민서 필터 변경 시 활동/업무 집계 변화.
- 데스크톱 및 390px 모바일 화면 확인. 작은 화면의 자료/대화/체크리스트는 세로 배치.
- 검사한 화면의 브라우저 console error 없음.

## 다운로드 검증 범위

다운로드는 Blob URL과 브라우저의 표준 `download` 링크로 구현했습니다. UI 실행 중 콘솔 오류는 없었으나 Codex 내장 브라우저 자동화의 download 이벤트가 제한 시간 내 전달되지 않아 OS에 저장된 결과 파일까지 자동 확인하지는 못했습니다. 별도 Chrome 검사는 해당 테스트 탭 연결이 종료되어 완료하지 못했습니다. 실제 시연 환경의 Chrome/Edge에서 다운로드 저장 위치를 확인하는 최종 점검이 남아 있습니다.

## 리뷰 보완

1. 편집창이 열린 동안 들어온 대화/첨부가 스냅샷 저장으로 소실될 수 있었던 문제를 최신 단계 ID 기반 구조 병합으로 수정했습니다.
2. 아직 시작하지 않은 단계에서 되돌리며 진행 중 단계가 복수로 생기는 경로를 차단했습니다.
3. 완료 단계의 체크리스트 변경을 제한하고, 미리 열린 항목 추가 폼도 완료 이후 숨기며 최신 상태에서도 변경을 차단합니다.

## 범위 밖

사내 서버의 실제 모델 응답, 외부 업무시스템 연결, 서버 감사증적, 다중 사용자/다중 탭 충돌 해결, 인증·권한, 실제 GMP 프로세스 적합성, 실제 배포·DB 조회는 검증 대상이 아닙니다. 이 데모는 한 브라우저 탭에서 업무 흐름을 설명하는 용도입니다.

## Task 보드 / API 확장 검증

- 별도 테스트 origin `http://127.0.0.1:5174`에서 수행하여 사용 중인 5173 브라우저 기록을 변경하지 않았습니다.
- 전체 업무 8개가 모듈 열/완료 열에 중복 없이 표시됩니다. FDS 현재 단계 필터 2개, FDS 포함 필터 8개, ET 변경 관리 교차 필터 2개를 확인했습니다.
- 완료 필터를 포함 범위로 전환해도 완료 업무 2개가 유지됩니다.
- 작성자를 이희준으로 전환하여 팀 의견 저장 시 응답이 생성되지 않고, 박비오의 명시적 요청 후 샘플 응답이 생성됩니다.
- QA 검토 회의 대화방 생성 시 기본 대화 메시지가 나타나지 않습니다. 새로고침 후 마지막 대화방과 대화별 모델이 복원됩니다.
- Task 기본 모델 qa-review-model, 대화 override glm-5.2를 별도로 설정하여 구분을 확인했습니다.
- 로컬 테스트 endpoint의 모델 목록 JSON 오류 및 대화 HTTP 404 표시를 확인했습니다. 샘플 응답으로 대체하지 않습니다.
- 새 IMPACT 모듈에 2개 달성 기준 설정 → 템플릿에 추가 → 전체 수동 전환 → 새 업무 생성 → 해당 Task의 0/2 체크리스트와 정확한 기준 문구 승계를 확인했습니다.
- 390px 모바일에서 페이지 가로 넘침 없이 보드 내부 가로 스크롤을 확인했습니다. 데스크톱 DOM 폭도 확인했습니다. 검사한 화면의 수집 console error는 0개입니다.
- 리뷰에서 발견한 완료 필터 범위 변경 오류, 데이터 초기화 후 지연 응답 혼입 위험을 수정하고 회귀 테스트를 추가했습니다.
- API 성공 응답/인증 헤더/모델 ID/대화방 이력 분리는 mocked fetch 단위 테스트로 검증했습니다. 사내 주소와 인증 정보가 없어 실제 내부 모델 및 skill/knowledge 실행은 미검증입니다.
