# MES Agent Hub — 세션 인수인계

갱신일: **2026-09-24 (KST)** · 기능 코드 기준: **`f99be7c`** · 통합 브랜치: **`main`** · 개발 이력: **`codex/conversation-tag-hub`**

이 문서는 새 세션에서 대화 기록 없이 개발을 재개하기 위한 현재 상태 설명이다. 이전 계획과 다른 내용이 있으면 아래의 **확정된 제품 원칙과 실제 구현 상태**를 우선 확인한다. 실제 조직의 참고자료·카탈로그·담당자·주소는 담지 않는다.

**2026-09-26 참고 문서:** [Codex 데모 융합 아이디어·설계 비교](docs/codex-demo-integration-design-comparison.md). 사용자와 개선안을 논의했으나 최종적으로 **구현하지 않고 비교 자료로만 남기기로 종료**했다. 이 문서는 실행 지시나 구현 현황이 아니며, 기존 기능 상태를 변경하지 않는다.

## 1. 다음 세션의 시작점

1. 저장소는 `mes-assistant-platform`이다. 비교용 형제 저장소는 수정 대상이 아니다.
2. `git status --short`, `git branch --show-current`, `git log -5 --oneline`으로 상태를 확인한다. 다른 세션의 미커밋 변경이 있으면 보존한다.
3. **최신 `main`이 통합 기준이다.** 새 기능 작업은 최신 `main`에서 목적에 맞는 `codex/` 브랜치로 시작한다. 새 중첩 프로젝트나 별도 앱 폴더를 만들 필요가 없다.
4. 루트에서 앱이 실행된다. 대화·태그 개편과 파일/체크리스트 연동, 핸드오프가 `main`에 병합되었다.
5. 본문의 미완료 항목을 확인하고 필요한 작업부터 진행한다. 이미 구현된 개편을 처음부터 다시 만들지 않는다.
6. 사용자 데이터가 있는 브라우저 저장소를 초기화하지 않는다. 변경 전 전체 백업을 받는다.

### Git 경계

| 커밋 | 내용 | 상태 |
|---|---|---|
| `cd254f5` | 새 앱을 루트로 통합, 이전 앱 보관 | 완료 |
| `fba4a52` | 루트 통합을 `main`에 병합 | `main` push 완료 |
| `84a3c71` | 대화·태그 중심 개편, 가상 기본 카탈로그 | 기능 브랜치 push 완료 |
| `f99be7c` | OpenWebUI 파일 어댑터, 선택적 AI 체크리스트 점검 및 리뷰 수정 | 기능 브랜치 push 완료 |

핸드오프 초안은 `be25af4`로 기능 브랜치에 push했다. 이후 사용자의 명시적 요청으로 `main`에 merge commit 방식으로 통합하고 이 문서의 재개 기준을 갱신했다. 통합 커밋은 `git log --merges -1`로 확인한다. 개발 브랜치는 이력 확인용으로 보존한다. 공유 이력 재작성·강제 push는 하지 않는다.

## 2. 제품 컨셉과 확정된 결정

**플랫폼은 대화창과 파일을 주고받는 환경을 제공한다. 업무 절차·전문 판단·질문 순서·skill·knowledge는 연결된 assistant가 담당한다.**

| 주제 | 유지할 결정 |
|---|---|
| 업무 단위 | 대화 하나가 업무 하나다. 에이전트 카드에서 바로 입력한다. |
| 생성 시점 | 첫 메시지 또는 첨부 저장 시 생성한다. 빈 화면 방문만으로 업무를 만들지 않는다. |
| 업무 탐색 | 카드 갤러리와 전체 업무 칸반을 전환한다. 칸반의 열은 **에이전트**다. |
| 분류·상태 | Lv1/Lv2는 탐색 분류다. 업무 상태는 카드 배지이며 assistant 실행 단계가 아니다. |
| 자료 연결 | 다중 태그로 직접 연결된 원본 자료를 발견한다. 자동 전달·자동 실행은 하지 않는다. |
| AI 사용 | 사용자가 체크한 파일 버전만 보낸다. 업로드·태그 연결 자체는 전송이 아니다. |
| 주 입력 | 체크된 자료에 별표를 추가한다. 별표 해제는 선택을 유지하고 선택 해제는 별표도 해제한다. |
| 공개 범위 | 태그는 권한을 부여하지 않는다. 요청자는 본인 접수 대화와 명시적으로 공유한 결과만 본다. |
| 체크리스트 | 선택 사항이고 기본값은 비어 있다. 명시적 AI 점검은 체크를 반영할 수 있으나 최종 완료는 사람이 결정한다. |
| API 실패 | 오류를 표시한다. 샘플 응답이나 텍스트 전달로 자동 대체하지 않는다. |
| 기본 데이터 | **가상의 assistant 3개만 제공한다.** 실제 카탈로그·사용자는 사용자가 등록한다. |

일반 대화에 업무용 역할 지침을 추가하지 않는다. 제목 제안과 체크리스트 평가는 별도의 보조 호출이다. 과거 계획의 실제 카탈로그 12개, 업무별 샘플 질문 시나리오, 고정 워크플로우 구현은 현재 기준이 아니다.

## 3. 구현 상태 — 화면에서 확인할 수 있는 것

| 영역 | 구현된 동작 | 경계 |
|---|---|---|
| 갤러리·관리 | 반응형 카드, 이미지/이니셜, 검색·분류, 에이전트 설정·모델 매핑 | 실제 assistant 주소·정보는 사용자 등록 |
| 공통 순서 | System Owner의 드래그/이동 버튼, 저장·취소, revision 충돌 감지 | 필터 없는 전체 목록에서 편집; 갤러리/칸반 공통 |
| 즉시 대화 | 카드 진입, 최근 대화, 새 대화, 첫 저장의 원자적 생성 | 첨부와 함께 작성한 텍스트도 보존; 첨부 저장만으로 AI 호출하지 않음 |
| 전체 칸반 | 에이전트 열, 최근 활동순, 상태·담당자·태그·보관 필터, 열 점프·접기·빈 열 숨기기 | 열 이동으로 assistant를 바꾸지 않음; 화면 설정 사용자별 보존 |
| 태그 | 자동완성, 키워드/SR 명시적 유형, 중복 방지, SR 색상 | 기존 SR 번호 등록은 신규 접수가 아님 |
| 자료함 | 입력/산출물, 버전, 출처, 미리보기·다운로드, 선택·주 입력 | 발견된 모든 파일이 AI에 전달되는 것은 아님 |
| SR | 제목 없이 접수 대화, 명시적 접수, 번호·태그·알림, 보호된 AI 제목, 후속 대화 | 업무 상태와 SR 상태 독립; 후속 대화에 자료를 자동 선택하지 않음 |
| 업무 기록 | 팀 의견, 체크리스트·메모, 활동 이력, 완료 기록·잠금, 사유 있는 재개 | 팀 의견과 assistant 호출을 구분 |
| 부가 기능 | 리포트, 결과/개선 자료 내보내기, 시스템 보조, 직접 접근 링크 | 집계·시연 기능이며 업무 판단이나 동기화 서버가 아님 |
| API | 일반 Chat Completions/OpenWebUI, 모델 우선순위, 취소·실패·재시도 | 실제 사내 서버 검증은 미실시 |

분류 필터는 같은 항목 내 OR, 서로 다른 항목 간 AND다. 칸반 카드 정렬은 최근 활동, 생성 시각, ID 순으로 안정화한다. SR가 여러 개면 연결 순서상 첫 SR가 카드 표시선 색상을 정하고 모든 SR 칩은 유지한다.

## 4. 자료 공유와 데이터 불변 조건

### 발견 → 선택 → 전송을 분리한다

- 자료함에는 현재 업무의 원본 자료와, 현재 업무와 태그를 **직접** 공유하는 원본 업무의 자료를 표시한다. 같은 버전은 한 번만 표시한다.
- A–B, B–C만 연결된 경우 A는 C 자료를 발견하지 않는다. B에서 C 파일을 선택해도 파일 소유 출처는 C다.
- B가 새 답변을 산출물로 저장하면 그것은 B의 새 Output이다. 가져온 파일 자체를 새 Output으로 바꾸지 않는다.
- assistant 답변의 명시적 저장은 Markdown Output이다. 사용자 질문·팀 의견·메모 등의 저장은 Input이다. 원본 메시지 ID와 작성자 정보를 보존한다.
- 태그를 해제하면 발견 목록이 바뀐다. **이미 선택한 입력 버전은 유지**되고 사용자가 별도로 해제한다.
- 새 버전은 알림/선택 대상이며 기존 선택을 교체하지 않는다. 원본 업무 보관도 선택 버전을 변경하지 않는다.
- 요청 시작 시 선택 자료·주 입력·모델·연결 설정을 고정한다. 전송 중 화면 변경은 진행 중 요청에 영향을 주지 않는다.
- 이전 요청의 원문 스냅샷을 대화 이력이라는 이유로 다시 붙이지 않는다. 과거 답변과 전송 기록은 남는다.

자료 출처를 표시할 때도 접근 검사를 해야 한다. 공유된 산출물을 볼 수 있다는 이유로 내부 원본 대화나 메시지 전체를 노출해서는 안 된다.

### 저장소와 모델

| 이름 | 실제 구현 |
|---|---|
| 브라우저 DB | IndexedDB `mes-agent-hub-v3`; Dexie 사용 |
| 버전 구분 | `HubState.version = 1`은 기존 외피, `hubVersion = 3`은 제품 계약, **Dexie schema version = 2**는 추가 테이블 마이그레이션이다. 서로 같은 번호가 아니다. |
| 업무/대화 | 기존 `WorkItem`·`Conversation` 타입을 유지하고 새 데이터에서 1:1 관계를 보장한다. |
| 태그 | `Tag`, `TaskTag`; 유형+정규화 키 및 업무+태그의 unique index |
| 파일/선택 | `ArtifactVersion`, `TaskInput`; 파일 버전은 불변, 선택은 업무+파일 버전별 하나 |
| 전송/완료 | `RequestRecord`, `CompletionSnapshot`; 당시 선택·상태를 남긴다. |
| 평가 | `ChecklistAssessment`; 평가 대상·결과·점수·자동 반영 여부·변경 전후 저장 |
| 복원 경계 | `meta.ready.epoch`가 복원 전 탭의 늦은 저장을 차단한다. |

태그 비교 키는 유니코드 NFKC 정규화, 앞뒤 공백·선행 `#` 제거, 연속 공백 정리, 영문 소문자화를 적용한다. 유형이 다르면 별도 태그다. 하이픈과 숫자의 선행 0은 보존한다.

기존 v2 DB에서 v3로 변환할 때 원본 DB는 남기고 데이터·파일을 원자적으로 복사한다. 기존 다중 대화 업무는 대화별 업무로 분리하며, 공용 자료·메모·체크리스트는 최소 변환하고 `legacyWorkId`로 출처를 남긴다. Output 여부는 기존 `outputIds`로 판단하고 생성 대화가 불명확하면 추정하지 않는다. 과거 묶음·인계 기록은 보존하지만 복잡한 호환 화면은 만들지 않았다.

전체 백업은 태그·순서·선택·평가·완료 기록과 파일 원문을 포함한다. 이전 형식 호환, 참조·체크섬 검사, 트랜잭션 복원을 제공한다. API 키와 메모리의 원격 파일 캐시는 제외한다. 전송/평가 중에는 백업·복원을 제한한다. **백업은 사용자 자료이므로 Git에 넣지 않는다.**

## 5. API 전송 경로와 실제 제한

### 공통 규칙

- 모델 적용 순서: **대화 지정 → 에이전트 매핑 → 연결 프로필 기본값**. 실제 API 모델이 비었거나 `default`면 설정을 안내한다.
- API 키는 메모리에만 있다. 새로고침하면 다시 입력해야 한다.
- 일반 채팅은 `stream: false`, 응답은 `choices[0].message.content` 문자열을 기대한다. 실제 응답 모델과 API/샘플 출처를 기록한다.
- 샘플 모드의 실행 경로는 단순 대역이며 실제 분석이 아님과 선택 자료 수를 표시한다.
- 일반 API는 선택한 텍스트를 참고용 `user` 메시지로 보낸다. 텍스트가 없는 바이너리는 메타데이터만 전달하므로 분석 지원으로 표현하지 않는다.

### OpenWebUI

1. 사용자가 assistant를 호출하면 선택한 원본 파일을 `POST /api/v1/files/`로 전송한다.
2. `GET /api/v1/files/{id}/process/status`를 2초마다 확인한다. 파일별 최대 5분, `completed` 이후만 다음 단계로 간다.
3. 전부 성공하면 `/api/chat/completions` 요청에 `files: [{type: "file", id}]`를 붙인다. 경로는 연결 프로필에서 변경 가능하다.
4. 파일 실패·취소·시간 초과이면 채팅을 중단한다. 제외·재시도·읽을 수 있는 자료의 텍스트 재시도는 사용자의 명시적 조작이다.

파일은 순차 처리한다. 현재 호출 전체 제한은 OpenWebUI 6분, 일반 API 60초다. 여러 대용량 파일을 사용하면 파일별 제한보다 전체 제한에 먼저 도달할 수 있다. 파일 선택 해제는 다음 요청에서 제외하는 동작이며 서버 파일 삭제가 아니다.

원격 파일 ID 캐시는 메모리에만 있다. DB epoch·사용자·역할·프로필 ID·서버·업로드/처리 경로·키의 해시와 불변 파일 버전 ID로 범위를 나눈다. 이 조건이 바뀌면 기존 항목을 재사용하지 않는다. 캐시를 백업하거나 API 키를 DB로 옮기지 않는다.

**실제 설치 버전의 상태 응답·파일 분석·assistant ID 매핑·인증·CORS는 아직 검증하지 않았다.** 임의 답변 URL을 자동 다운로드하거나 외부 대화를 자동 수집하지 않는다. 외부 링크 열기와 플랫폼 내부 API 대화는 별개다.

## 6. 선택적 AI 달성도 점검

- 버튼을 누를 때만 별도 보조 요청을 실행한다. 일반 대화 메시지에 점검 지시나 결과를 추가하지 않는다.
- 대상은 현재 체크리스트, 열람 가능한 현재 대화의 일반 메시지, 선택 자료다. 팀 의견 메시지는 현재 평가 입력에서 제외된다.
- 모델 우선순위와 파일 어댑터는 대화와 같다. 샘플 모드는 모든 항목을 `판단 불가`로 돌려 실제 평가처럼 보이지 않게 한다.
- 각 항목을 정확히 한 번 반환하는 JSON을 검증한다. `achieved / unmet / unknown`, 근거 문자열, 제공된 메시지/파일 ID만 참조하도록 검사한다.
- 달성은 체크, 미달성은 해제, 판단 불가는 기존 체크 유지다. AI 평가 m/n과 사용자가 수정할 수 있는 현재 체크 m/n을 구분한다.
- 적용 직전에 체크리스트·대화 메시지 ID·선택 파일 ID·주 입력 플래그·완료 여부·복원 epoch를 비교한다. 변경되었으면 결과를 보존하되 자동 반영하지 않는다.
- 오류·취소·잘못된 JSON은 체크를 바꾸지 않는다. 완료 업무에는 적용하지 않고 업무/SR 상태도 변경하지 않는다.

평가는 전용 테이블과 7분 임대를 사용한다. 일반 채팅의 heartbeat 요청 서비스와 완전히 하나로 통합된 구조는 아니다. 만료 평가 복구와 역할 전환 시 취소는 구현되어 있다. 현재 JSON 파서는 Markdown 코드 펜스를 제거하지 않으므로 실제 모델의 구조화 응답 호환성을 확인해야 한다.

## 7. 코드 탐색 지도

| 수정하려는 영역 | 시작 파일 |
|---|---|
| 앱 구조·SR·보고 | [src/App.tsx](src/App.tsx) |
| 갤러리·전체 칸반·공통 순서 | [src/HubHome.tsx](src/HubHome.tsx) |
| 즉시 대화·첫 저장 | [src/NewConversation.tsx](src/NewConversation.tsx), [draftService.ts](src/app/draftService.ts) |
| 대화·자료·체크리스트 UI | [src/Workspace.tsx](src/Workspace.tsx) |
| 태그·자료함 | [src/TagEditor.tsx](src/TagEditor.tsx), [src/MaterialLibrary.tsx](src/MaterialLibrary.tsx) |
| 에이전트·API·백업 관리 | [src/AgentAdmin.tsx](src/AgentAdmin.tsx) |
| 가상 초기 데이터 | [src/demoCatalog.ts](src/demoCatalog.ts); `src/seed.ts`는 기존 테스트/전환용 데이터와 구분 |
| 데이터 계약·발견 규칙 | [src/types.ts](src/types.ts), [src/hub.ts](src/hub.ts) |
| 권한·명령·충돌 | [src/domain.ts](src/domain.ts), [commands.ts](src/domain/commands.ts), [DB commands](src/db/commands.ts) |
| 요청 구성·실행 | [src/api.ts](src/api.ts), [requestService.ts](src/app/requestService.ts) |
| 파일 API 어댑터 | [openWebUI.ts](src/app/openWebUI.ts) |
| AI 점검·SR 제목 | [checklistAssessment.ts](src/app/checklistAssessment.ts), [titleService.ts](src/app/titleService.ts) |
| 로딩·세션·복구 | [src/store.tsx](src/store.tsx), [session.ts](src/app/session.ts) |
| DB·변환·백업 | [schema.ts](src/db/schema.ts), [migrateV3.ts](src/db/migrateV3.ts), [backup.ts](src/db/backup.ts), [validate.ts](src/db/validate.ts) |
| 브라우저 검증 | [scripts/browser-hub.mjs](scripts/browser-hub.mjs), [browser-smoke.mjs](scripts/browser-smoke.mjs), [browser-lifecycle.mjs](scripts/browser-lifecycle.mjs) |

네트워크 작업은 IndexedDB 트랜잭션 밖에서 한다. 저장 시 요청 임대/토큰·revision·epoch를 재검사하는 구조를 유지한다. UI 파일에 API를 우회 호출하는 경로를 추가하기 전에 서비스 계층을 확인한다.

## 8. 실행·검증 기록

Node.js 22 이상, React 19·TypeScript 5.9·Vite 7·Dexie 4 기반이다. 저장소 **루트**에서 실행한다.

```powershell
npm ci
npm run dev
# 다른 터미널에서
npm test
npm run build
npm run test:browser
npm run test:browser:lifecycle
npm run test:browser:hub
```

개발 주소는 `http://127.0.0.1:5180`이고 포트가 사용 중이면 임의로 다음 포트로 넘어가지 않는다. Preview는 `npm run preview`로 4180이다. 브라우저 검증은 설치된 Microsoft Edge와 5180 개발 서버를 사용하며 격리된 컨텍스트·가상 자료·모의 API로 실행한다. 기존 앱 `legacy/flowmes`는 기본 테스트 대상에서 제외한다.

### 코드 기준 `f99be7c`에서 완료한 검증 (2026-09-23)

| 검증 | 결과 / 확인 내용 |
|---|---|
| `npm test` | **15개 파일, 62개 테스트 통과** |
| `npm run build` | TypeScript 및 Vite 프로덕션 빌드 통과 |
| `test:browser:hub` | 빈 업무 방지, 첨부 동반 텍스트, SR 후속 업무·태그·선택/별표, 요청 모델, 모의 파일 업로드/처리/첨부, AI 점검, 순서 편집, 13개 이상 열·복원 |
| `test:browser` | 두 탭 저장, 탭별 역할, 느린 요청의 고정 입력, 선택 해제, 모델 기록, 실패/재시도/취소, 요청자 공개 범위, 재로딩, 백업 다운로드 |
| `test:browser:lifecycle` | 명시적 SR 접수·결과 공유, 완료 잠금·재개, 전체 백업 복원, 오래된 탭의 저장 차단 |
| 독립 코드 리뷰 | 아래 네 항목 수정 후 회귀 검증 |
| 실제 사내 API | **미실시**. 모의 API 통과를 실제 연결 검증으로 해석하지 않는다. |

리뷰에서 수정한 문제: 첨부를 먼저 저장할 때 작성 텍스트 유실, 평가 중 근거가 변경된 결과의 자동 반영, 기존 공용 Output의 Input 오분류, 평가 진행 중 복원 허용. 출처 보기에서도 내부 메시지 접근 검사를 적용했다.

핸드오프 추가는 문서 변경이다. 위 브라우저 결과는 해당 기능 코드 커밋에서 수행한 기록이다. 2026-09-24 `main` 통합 시 단위/통합 테스트 62개와 프로덕션 빌드를 다시 확인했다. 문서 갱신으로 기능 코드는 변경하지 않았으며 브라우저 3종 검증은 2026-09-23 실행 기록을 유지한다.

## 9. 남은 사항과 다음 작업 우선순위

### A. 실제 연결 전에 필요한 확인

1. 사용자가 등록한 테스트용 프로필로 실제 OpenWebUI 업로드·처리 상태·파일 첨부 형식·모델/assistant ID·인증·CORS를 확인한다. 키나 내부 주소를 테스트 코드/문서에 기록하지 않는다.
2. PDF·Office·이미지의 실제 분석 가능 여부와 복수 파일 처리 시간을 확인한다. 현재 프론트가 자체 파싱을 제공한다고 설명하지 않는다.
3. 실제 모델의 평가 JSON·참조 ID 응답과 제목 제안을 검증한다. 실패 시 사용자 체크/제목이 보존되는지도 확인한다.

### B. 코드 분석에서 확인한 보완점 (이번 문서 작업에서는 수정하지 않음)

| 우선순위 | 현재 코드와 보완 방향 | 검증 목표 |
|---|---|---|
| 높음 | **OpenWebUI 일반 대화에 주 입력 별표 의미가 전달되지 않는다.** 로컬 선택/전송 스냅샷에는 `main`이 있지만 어댑터의 파일 첨부는 `type/id`뿐이고 일반 메시지에도 별도 표시가 없다. 일반 텍스트 모드는 `[주 입력]`을 전달한다. 파일명/버전/역할 메타데이터를 참고용 메시지로 전달하는 방식 등을 검토한다. | 별표 선택이 실제 OpenWebUI 요청에 식별 가능하게 반영되며 원문 중복 삽입·역할 지침 주입은 없음 |
| 보통 | 어댑터가 반환한 원격 파일 ID를 일반 요청 서비스가 최종 감사 기록에 저장하지 않는다. 현재 기록은 로컬 파일 버전·주 입력·프로필·단계 중심이다. 정확한 원격 전송 추적이 필요하면 매핑 기록을 확장한다. | 백업에 API 키/재사용 캐시를 넣지 않으면서 전송 당시 첨부를 추적 |
| 보통 | 평가가 일반 요청 서비스와 별도 임대/취소 경로를 사용한다. 계획의 완전한 공통 요청 수명 관리와는 차이가 있다. 추후 통합 시 현재 충돌 보호를 유지한다. | 긴 요청·탭 종료·취소·복원·재시도 회귀 테스트 |
| 낮음 | `src/api.ts`의 이전 `callAssistant` 보조 함수에는 구형 샘플 질문 문구가 남아 있다. 현재 업무 화면은 `requestService`를 사용한다. 사용처를 확인하고 정리하면 재사용 시 혼선을 줄인다. | 일반 실행 경로에 업무별 샘플 절차가 다시 들어가지 않음 |

이 표는 모든 회귀 테스트가 통과해도 남을 수 있는 계약 차이/유지보수 항목이다. “모든 요구사항 완전 충족”으로 바꾸지 않는다. 외부 링크 전용 에이전트의 AI 점검처럼 연결 프로필이 없는 조합도 추가 경계 테스트 후보이며, 재현 전에는 확정 결함으로 단정하지 않는다.

### C. 별도 요청 또는 운영 확장 범위

- 이번 개편의 `main` 병합은 사용자 요청으로 진행했다. 후속 변경은 새 기능 브랜치에서 검증하고 병합 범위를 별도로 정한다.
- 실제 인증·SSO·서버 권한 강제·다중 PC 동기화·외부 접수 시스템 연동은 데모 밖이다.
- 역할 전환은 UI 시연이며 보안 경계가 아니다. GMP 운영 적합성이나 검증 완료를 주장하지 않는다.
- 이미 과거 공개 이력에 있었던 정보의 제거는 별도 이력 정리 작업이다. 이번 작업에서 공유 이력을 강제로 다시 쓰지 않았다.

## 10. 공개 자료·운영 주의점

- 실제 참고 이미지, 실제 카탈로그·담당자·사내 주소, 사용자 백업을 코드나 문서에 옮기지 않는다. 가상 예시만 사용한다.
- 로컬 자료는 Git에서 제외된 `.local-only/`, `private-data/`, `reference-materials/` 등에 둔다. `.env*`도 제외되지만 **ignore 규칙만으로 안전하다고 가정하지 말고 staged diff를 확인한다.**
- 새 기본 데이터는 `demoCatalog.ts`다. 사용자 등록 에이전트나 기존 브라우저 데이터를 덮어쓰지 않는다. 옛 민감자료 포함 로컬 커밋을 복구·cherry-pick·push하지 않는다.
- 이전 원격 이력에는 당시 데이터가 남을 수 있다. 현재 스냅샷 정리와 Git 이력 정리는 다르다. 원문을 핸드오프에 재기재하지 않는다.
- `localhost`/`127.0.0.1`, 포트, 브라우저가 달라지면 별도 저장소다. 다른 PC에서 Git pull만으로 업무 데이터가 오지 않는다.
- “화면이 예전과 같다”면 브랜치·실행 폴더·포트·기존 사용자 데이터 보존 여부부터 확인한다. 먼저 DB를 삭제하지 않는다.
- 서버를 정리할 때 해당 저장소의 프로세스인지 확인한다. 형제 저장소의 개발 서버나 모든 Node 프로세스를 일괄 종료하지 않는다.

## 11. 복사해서 사용할 재개 요청

> `mes-assistant-platform`의 `HANDOFF.md`를 읽고 현재 Git 상태를 확인하세요. 기존 개편은 `main`에 통합되어 있으므로 최신 `main`을 기준으로 새 `codex/` 브랜치에서 후속 작업을 시작하고 이미 완료된 개편을 반복하지 마세요. 플랫폼은 대화와 선택 파일 전달을 담당하고 assistant 내부 업무 절차는 구현하지 않습니다. 실제 참고자료·카탈로그·담당자·주소·백업을 Git에 넣지 마세요. 섹션 9의 실제 연결 미검증과 코드 보완점을 구분하여 다음 작업을 정하고, 변경에 맞는 회귀 검증 후 작업 브랜치에 commit·push하세요. 후속 변경의 `main` 병합과 공유 이력 재작성은 별도 요청 없이 수행하지 마세요.

관련 기록: [실행 안내](README.md), [개편 계획 및 진행](docs/conversation-tag-plan.md), [리뷰 기준 및 결과](docs/conversation-tag-review.md), [루트 통합](docs/root-integration.md).
